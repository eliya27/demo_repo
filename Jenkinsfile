pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Check Image'
            }
        }
    stage('Build') {
            steps {
                echo 'Build Image'
            }
        }    
    }
}
